项目问题：
页面跳转发生闪烁
json可以传字符串，浏览器可以把字符串当成对象解析

特点使用了：layer的script

JS中使用var声明的就是局部
不使用就是全局。前面有一个window的隐藏

前台路径：<href=""><img src="">相对服务器的根：http://localhost:8080/ssss
后台路径：forward,xml,相对路径的，包括项目名字

分页查询：
1.js中写数据
2.ajax

模糊查询：
sql的where用法

批量删除：
表单序列化

登录拦截器
